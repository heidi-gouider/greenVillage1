{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
	<div class="contenu">

		<h2 class="fst-italic text-white text-center">Mon profil</h2>

		{# <div class="container mx-auto text-white mt-5" id="formulaire"> #}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    {# <h3 class="card-title">Profil de {{ app.user.username }}</h3> #}
                    <h3 class="card-title">Profil de {{ app.user.email }}</h3>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        {# <div class="col-md-3">
                            <strong>Nom d'utilisateur :</strong>
                        </div>
                        <div class="col-md-9">
                            {{ app.user.username }}
                        </div> #}
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-3">
                            <strong>Email :</strong>
                        </div>
                        <div class="col-md-9">
                            {{ app.user.email }}
                        </div>
                    </div>
                    {# Ajoutez d'autres informations selon vos besoins (par exemple, nom, prénom, etc.) #}
                    {# Exemple avec un lien pour modifier le profil #}
                    <div class="row mt-4">
                        <div class="col-md-12">
                            <a href={# "{{ path('app_profil_edit') }}" #} class="btn btn-primary">Modifier le profil</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
        </div>
</div>
{% endblock %}